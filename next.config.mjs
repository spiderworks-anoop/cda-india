/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,

  images: {
    domains: ["cda-sa.spider.ws"],
  },
  reactStrictMode: false,
  images: {
    domains: [ "cda-sa.spider.ws" ],
  },
};

export default nextConfig;
